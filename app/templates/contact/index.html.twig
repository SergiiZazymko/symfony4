{% extends 'base.html.twig' %}

{% block title %}iShop | Contact{% endblock %}

{% block body %}

    {% form_theme form 'bootstrap_4_layout.html.twig' %}

    <div class="form-control">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success" value="Send message">
        {{ form_end(form) }}
    </div>

{% endblock %}
